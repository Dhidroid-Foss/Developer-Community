/**
 * components/common/index.ts — barrel export for shared primitive components.
 *
 * Import from here instead of individual files:
 *   import { Eyebrow, DeviconIcon, makeIcon } from "@/components/common";
 */

export { Eyebrow } from "./eyebrow";
export { DeviconIcon, makeIcon } from "./DeviconIcon";
